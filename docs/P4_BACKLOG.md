# PHASE 4 Backlog — kitty-kaki-survivors

Author: Claude C  ·  Opened: 2026-05-18  ·  Advisor-ordered sequence

## Sequencing (advisor)

1. **P4J Telemetry harness** — solo, unblocks all downstream balance/NG+/daily/Cave decisions. Ship FIRST.
2. **P4G Audio mixer** + **P4H Accessibility** — foundation, harder to retrofit after Cave doubles audio/palette surface.
3. **P4D NG+ modifiers** + **P4F Char unlock chain** + **P4E Daily seed leaderboard** — replay multipliers on existing forest, near-zero new asset work.
4. **P4K Stage-author docs** authored *alongside* **P4A Cave skeleton** — Cave becomes canonical template. Cave = full pro-asset commit (~20–30 cohorts to match forest texture coverage: sky, ground normal, walls, stalactites, chest, coffin, neutrals, hazards, weapons, music phases, achievements).
5. **P4I Balance pass** — last, requires P4J data + play sessions.

## Constraints (must honor)

- **Pro-asset rule applies to Cave.** Raw-geometry Cave = debt. Either commit to full texture push or pause Cave.
- **Cohort parallelism cap: 2.** Different files only. NG+ + Cave touch overlapping run/stage code → serialize.
- **Cron exit explicit.** Each cron tick checks acceptance; if all items in current phase meet acceptance → emit `iteration tick: P4 fully shipped, awaiting user direction` and stop.
- **Smoke gates unchanged:** `smoke-sig-weapons`, `smoke-sprite-fx`, `smoke-forest-v2` must pass per merge.

## Acceptance criteria (one line per item — must be checkable)

| ID | Item | Acceptance |
|----|------|-----------|
| P4J | Telemetry harness | ✅ 208b89a — `src/telemetry.js` collects per-run stats (duration, kills, picks, deaths, weapons taken, evolves) → localStorage `kks_telemetry`. Export JSON button on end-run summary. Schema v1. smoke-p4j-telemetry passes. |
| P4G | Audio mixer | ✅ 9d27047 — 4 sliders (master/music/sfx/ambient) in options modal (Escape pause + main-menu Settings both route to showOptions; no separate pause DOM in repo). Persist to localStorage. All 12 SFX route through _sfxBus; 5 forest day/night music tracks + flat stage ambient loops route through new _ambientBus; procedural in-run music + menu bed remain on _musicBus. Mute = 0 amplitude (play() still fires; _playCounts increments under gain=0). smoke-p4g-mixer verifies 4-bus existence + mute-≠-skip + localStorage round-trip + schema readability. |
| P4H | Accessibility | ✅ efee0d9 — (a) Colorblind palette toggle (deuteranopia variant of forest 8-color), (b) screen-shake 0–100% slider, (c) hold-to-confirm toggle on irreversible menu actions. All persist. Smoke loads w/ each toggle off+on. |
| P4D | NG+ modifiers | ✅ def6428 — 3 modifiers unlock on first forest clear (state.victory OR reaperOutlasted edge in src/forestAchievements.js): Mirror Mobs (+50% spawn via swarmMul in src/spawnDirector.js), Twin Bosses (paired adjacent boss in spawnMiniBoss + spawnFinalBoss, _isTwin tag on the live enemy), Half Pickups (50% pre-gate in src/forestPickups.js dropForestPickup). Toggle pre-run from menuV2 continue card (forest-only, greyed when !meta.unlockedNgPlus). state.modes mirror in main.js applyMetaUpgrades so telemetry beginRun auto-tags the trio. smoke-p4d-ngplus drives tickSpawnDirector directly + clean-pool probe for half-pickup; baseline vs each flag independent. |
| P4F | Char unlock chain | ✅ 4dbc7ac — 3 hidden AVATARS (rune/mire/shroud kitten) wired in src/config.js with `unlock` = bare achievement id. 2 new forest achievements (`no_hit_clear` = forest win at dmgTaken===0; `rings_dodged_100` = count puff-cycles where hero stayed outside SPORE_PUFF_RADIUS) plus reused `reaper_outlasted` (35:00 outlast — acceptance "60s" was loose; 35:00 keeps gate consistent with P4D NG+ trigger). Generic avatar-unlock funnel in forestAchievements.js#unlockAchievement (first-lifetime-unlock-gated, skips `flag:`/`sigils:` forms to avoid over-match). Toast reuses the gold-bordered slide pipeline with a "CHARACTER UNLOCKED" prefix variant. Ring-dodge counter bumps at MP_PUFF→MP_IDLE transition in src/forestEnvHazards.js (edge-fire, not per-frame). smoke-p4f-charunlock passes 5 assertions (config wiring, 3 per-avatar cycles, idempotency, over-match guard) with 0 pageerrors. |
| P4E | Daily seed leaderboard | ✅ cc8d978 — New `src/dailyRng.js` (mulberry32(YYYYMMDD)) with `seedDaily`/`clearDailySeed`/`rand` seam; main.js seeds in applyMetaUpgrades when `state.modes.daily` is true and clears in _teardownActiveRun + defensively in initSpawnDirector for non-daily runs. spawnDirector.js routes 12 spawn-decision Math.random call sites (angles, ring jitter, weightedPick, miniboss/finalboss/nemesis/horde) through the seeded seam; cosmetic per-mesh randoms in enemies.js (hue/mixerPhase/rangedCD) intentionally left native. leaderboard.js: `recordRun` stamps `dailyDate` only when mode='daily'; new `topDailyForSeed`/`topDailyToday`/`encodeShareCode`/`decodeShareCode` (unicode-safe btoa, returns null on malformed input). main.js mirrors the weekly run-end commit block for daily mode (`_dailyCommitted` one-shot). endRunSummary.js: daily-mode-only Share Daily button (clipboard + prompt() fallback) and inline top-3 daily leaderboard panel reusing the existing achievements-box style. smoke-p4e-daily.mjs passes 4 assertions including byte-for-byte spawn-position determinism across two browser contexts seeded with the same YYYYMMDD (requires pre-tick active-list drain to neutralize boot-time rAF jitter — director RNG itself is per-call deterministic). All 8 smoke gates pass. |
| P4K | Stage-author docs | ✅ 2026-05-20 — `docs/STAGE_AUTHORING.md` (203 lines) documents all required sections: palette decl (§1), STAGES shape (§2), builder (§3), preload (§4), main.js switch (§5), menu (§6), smoke (§7 + weapons-gate gotcha), weapon registration (§8a), neutral/hazard/landmark slots (§8b), music phase hooks (§8c), achievement registration (§8d), sky dome (§8e), ground normal (§8f), prior art (§9), multi-cohort cadence (§10). Cave files referenced as canonical throughout. The ✏️ "finalized at P4A-cN" markers are intentional co-evolution (the doc firms up as each Cave cohort lands the subsystem), not gaps — the patterns are documented now. |
| P4A | Cave stage (full pro-asset) | New stage `cave` selectable from menu. Full coverage: 5-color palette doc, 3 rooms, 2 forest-equivalent weapons, stalactite landmark, gloomshrimp neutral, cave-in hazard, sealed doors, 1 boss + 1 miniboss, 3-phase music, sky-dome equivalent (cave ceiling shader), ground normal, stone wall textures, achievements. smoke-cave-v2 passes 4 phases. |
| P4A | ↳ Cohort progress | **Cohort 1 of N shipped** (2026-05-18) — skeleton + palette doc + boot integration + smoke phase 1. STAGES entry, `src/stages/cave/{cavePalette,caveStage}.js`, main.js stage-resolve + dispose wire-up, assets.js preloadStage cave arm, menuV2 STAGE_ART card, `docs/CAVE_VISUAL_STYLE.md`, `tools/smoke-cave-v2.mjs` phase 1 only. P4K STAGE_AUTHORING.md draft co-shipped. **Cohort 2 of N shipped** (2026-05-18) — stalactite landmark cluster + env.js cave branch. New `src/stages/cave/caveStalactites.js` (mulberry32 seeded `0xC0CA0E1` → 6 author-anchored clusters × 4-5 stalactites = 26 InstancedMesh bodies + 26 bloom-tagged slot-3 moss-emissive tips, parented under `caveStage`). Removed cohort-1 Box-as-stalactite placeholder. `src/env.js`: ATMOS_SPECS cave entry (36 glowmoss spores, slot-3 color, slow upward drift via new `_tickCave`), applyStageTint cave arm (hemi.intensity 0.18, slot-3 leaning hemi sky 0x4c7a72, slot-1 hemi ground, dim slate sun 0.55, cool fill). smoke-cave-v2 extended with phase 2 (stalactiteCount ≥6 + bloom tag) + phase 3 (atmosClusters.cave visible + fog 0x1a1820 + hemi.intensity ≤0.22). **Cohort 3 of N shipped** (2026-05-18, 414b65a) — glowmoss floor decals + dedicated cave ground pack. New `src/stages/cave/caveGlowmoss.js` (mulberry32 seed `0xC0CA0E2` → 24 InstancedMesh CircleGeometry patches in the 12-26u annulus around hero spawn, MeshBasicMaterial slot-3 moss additive + bloom-tagged, polygonOffset {-1,-1} + renderOrder=-1 so hero+enemies occlude per [[fix_aoe_z_order]], 0.5 Hz alpha pulse 0.45→0.65 via shared-material `tickGlowmoss` exported on a new `tickCave` hook wired into main.js after tickAtmosphere). New `tools/_gen_cave_stone_texture.mjs` (deterministic mulberry32 procedural generator → `assets/textures/cave_stone_{diffuse,normal,rough}.png`, 256² each, palette-locked grayscale diffuse so slot-2 STAGES.cave.groundTint 0x4a4a52 drives the wet-stone hue; stronger normal STRENGTH 3.2 vs forest 2.5 for wetter relief; 40 pebble stamps + 12 crack hairlines). `src/env.js`: `groundPacks.cave` materialized via new `loadPngTex` helper, applyStageTint packKey ternary now `isCave ? 'cave' : (isForest ? 'forest' : 'twilight')` (drops the brown_mud fallthrough), cave-specific roughness 0.85, `envGroup.userData.groundPacks` exposed for smoke inspection. smoke-cave-v2 extended with phase 4 (glowmossCount ≥15 + bloom + renderOrder + polygonOffset + groundPacks.cave.diff truthy + not aliased to forest/twilight). All 11 smoke gates green. **Cohort 4 of N shipped** (2026-05-18, 3b56918) — ceiling-drip particle system. New `src/stages/cave/caveCeilingDrips.js` (24-slot InstancedMesh PlaneGeometry streaks, slot-3 moss CAVE_PALETTE.moss 0x7fffe4, MeshBasicMaterial additive blend + bloom-tagged, spawned from cohort-2 stalactite tips via new `getStalactiteTipPositions()` export on `caveStalactites.js`, gravity-fall g=9 m/s² → 0.3-0.6s flight from tip y=0.4-1.6 to y=0, landing splash-flatten via scale-Y collapse over 0.15s + slight XZ widen, slot recycle). Self-gated dispatcher 0.5 drips/s scaled by stalactite count (cap 1.0/s) — `nextSpawnAt` advances every cycle per [[feedback_kks_wave_dispatcher_throttle.md]] even when pool saturated or tips unavailable. No per-frame allocations: module-level `_dummy` Object3D + `_zeroMatrix` scratch (per cohort-4 advisor "no `new THREE.Vector3()` in tick"). Wired into existing `tickCave` hook in caveStage.js after tickGlowmoss; idempotent dispose chained from `disposeCaveStage`. Ambient SFX wire DEFERRED — `audio.js` exposes only `playStageAmbient(stageId)` (stage-bed switcher, not event hook for arbitrary ambient triggers), no drip sample under `assets/audio/` (no cave/, no drip/water match); spec said skip when both conditions hit (don't shim audio.js — P4G scope). smoke-cave-v2 extended with phase 5 (dripPoolSize ≥20 + bloom tag + additive blending + slot-3 color + `getCeilingDripTotalSpawned()` accessor ≥1 after 2.2s settle — counter-based probe sidesteps the flake of catching 0.3-0.6s flight particles mid-air per cohort-4 advisor). All 11 smoke gates green. **Cohort 5 of N shipped** (2026-05-20, abd42a4) — gloomshrimp neutrals (the cave's ambient-life equivalent of forest fireflies/deer per `src/forestNeutrals.js`). New `src/stages/cave/caveGloomshrimp.js` (seed `0xC0CA0E3`): 12 slot-2 stone bodies with slot-3 moss emissive (CAVE_PALETTE.moss 0x7fffe4) on BLOOM_LAYER, single ConeGeometry InstancedMesh. Drift at 0.7u/s with a deterministic sine-weave heading (no per-frame RNG — identical across daily seeds), dart away from the hero within 5u at 3.2u/s for 1.4s, soft annulus bound (9-24u) steers strays back toward center; Y-bob via shared anim clock. Non-combat (no XP/damage/progression). Zero-alloc tick (typed arrays + one scratch Object3D), reads hero pos from the shared state import, does not flee a dead hero (gameOver guard). Wired into caveStage build (records `userData.gloomshrimpCount`) / `tickCave` / dispose. smoke-cave-v2 phase 6: count≥10, InstancedMesh + bloom + slot-3 emissive, instance-0 movement across a 700ms gap (proves the tickCave→tickGloomshrimp chain is frame-wired). All 6 cave phases + smoke-sprite-fx/sig-weapons/forest-v2 green. **Cohort 6 of N shipped** (2026-05-20, 06cb1c0) — cave achievements (per-stage achievement file per STAGE_AUTHORING §8d). New `src/caveAchievements.js`: 5 cave chase goals (cave_enter / cave_gloomshrimp / cave_time_10min / cave_clear / cave_flawless_3min). New `registerExternalAchievements(defs)` seam in `forestAchievements.js` merges stage defs into the shared registry so they flow through the existing unlock→toast→meta→title-panel pipeline (idempotent per id). Eligibility scanned cave-only via the existing `tickCave` hook (no main.js edit); dedup owned by forestAchievements' run Set + lifetime meta. cave_gloomshrimp tie-in: caveGloomshrimp flags `state.run._caveShrimpStartled` on first hero-startle. smoke-cave-v2 phase 7: 5 defs registered + cave_enter auto-unlocked (frame-wired proof) + funnel idempotency. All 7 cave phases + smoke-sprite-fx/sig-weapons/forest-v2 green. **Cohort 7 of N shipped** (2026-05-20, 3a1efd8) — Gloomsigil, the first cave-stage weapon. New `src/weapons/gloomsigil.js`: plants a stationary violet sigil field (slot-4 0xc87bff) at the hero every cooldown; the field lingers 3-5.6s ticking DoT twice a second to enemies inside — a placed-DoT zone, mechanically distinct from frostbloom's instant pulse. VFX budget honored: reuses makeRuneRingTexture + fx.js spark pool, no new pool/texture. New stage-gate seam in `index.js#weaponChoices`: weapons with a `stages:[...]` field only enter the level-up pool on matching stages (no field = everywhere; carried weapons still tick anywhere). gloomsigil registered with `stages:['cave']` + descriptions blurb. smoke-cave-v2 phase 8 verifies the gate: REGISTRY + stages==['cave'] + forest offers=0 (no cross-stage leak) + cave offers≥1. **Cohort 8 of N shipped** (2026-05-20, b041e6b) — Echo Bolt, the 2nd cave weapon, which CLOSES the "2 forest-equivalent weapons" acceptance item. New `src/weapons/echobolt.js`: fires a sigil bolt at the nearest enemy every cooldown (pierce 1-4), `stages:['cave']`. Projectile mechanic, distinct from Gloomsigil's placed-DoT field. VFX budget honored — reuses autoAim's pooled projectile via `spawnAutoAimProjectile` (ownerWeapon 'echobolt'; central tick handles move/collide/damage), no new pool; cave flavor via a violet slot-4 muzzle spark + naming (pool art is ice-cyan, no per-cast color override). smoke-cave-v2 phase 9 mirrors the phase-8 gate test (forest offers=0, cave offers≥1). **Cohort 9 of N shipped** (2026-05-20, 8f8fce3) — perimeter stalagmite formations. New `src/stages/cave/caveStalagmites.js` (seed `0xC0CA0E4`): 8 gapped clusters × 4 = 32 InstancedMesh stone cones rising from the floor at r≈33-39, the mirror of the cohort-2 hanging stalactites and clear of the r≤26 decor footprint. **Satisfies the P4A "stone wall textures" acceptance item** via the cohort-3 cave_stone diffuse+normal maps (loaded mirroring `env.js#loadPngTex`; RepeatWrapping, repeat (2,3); `color = CAVE_PALETTE.stone` drives the palette-locked grayscale diffuse AND is the fetch-fail fallback; no roughness map → avoids the roughness=1.0 blind knob; no flatShading → the normal map needs smooth normals — all per advisor) — a stone-textured pro-asset, not flat placeholder. **Literal-reading note:** this satisfies the *texture* half via occlusion-safe perimeter pillars; a literal continuous *wall ring* was DEFERRED as a blind-tune trap (step 4d — no cave screenshot gate, ortho-iso (40,60,40) near-side occlusion + UV-stretch). A future cohort can still add a low knee-height wall ring if the user wants the literal reading; **the loop should NOT re-propose "stone wall textures" as unmet.** Static decor (no `tickCave` wiring); idempotent dispose tears down geo + mat + both textures. smoke-cave-v2 phase 10: stalagmiteCount ≥24 (built 32), stone-textured (map+normalMap), color 0x4a4a52, every instance r≥27 (perimeter, measured minR=30.6) + centerY≤3.0 (occlusion guard, measured 2.44 → tip ≈4.9). All 10 cave phases + sprite-fx/sig-weapons/forest-v2 green. **Cohort 10 of N shipped** (2026-05-20, 4a281b1) — perimeter glowmoss mushrooms. New `src/stages/cave/caveMushrooms.js` (seed `0xC0CA0E5`): 8×3 = 24 bioluminescent mushrooms clustered at the feet of the cohort-9 stalagmites (anchors mirror the stalagmite ring; scatter 2.5). Two InstancedMeshes — slot-2 stone stalk (flatShading, no emissive) + slot-3 moss-emissive cap dome (bloom). **Perimeter-ONLY (advisor):** a standing slot-3-emissive object in the playfield would be a third moss-glow source WITH a vertical silhouette = exactly what reads as a 3D pickup/XP-gem in a survivors-like; with no cave screenshot gate yet (CC7) that pickup-confusion can't be smoke-tested, so the flora lives entirely outside the pickup band (the slot-2 stalk does the silhouette disambiguation; height ≤0.9 = undergrowth, not a competing silhouette). Cave music was the alternative pick but is asset-blocked (no `assets/audio/cave/`; same defer as cohort-4 ambient — don't shim audio.js, P4G scope). Static decor (no tickCave). smoke-cave-v2 phase 11: mushroomCount≥24, stalk color 0x4a4a52 (NO slot-3 leak), cap emissive 0x7fffe4 + bloom, every instance r≥27 (pickup-band guard, measured minR=30.x), capY≤0.9. All 11 cave phases + sprite-fx/sig-weapons/forest-v2 green. **Cohort 11 of N shipped** (2026-05-20, 0e867b4) — central sigil-floor landmark. New `src/stages/cave/caveSigilFloor.js`: a large rune circle inscribed at hero spawn, filling the bare r<12 center (glowmoss patches start at the 12u annulus; perimeter has stalactites/stalagmites/mushrooms — the middle was empty). Reuses the game-wide `makeRuneRingTexture` tinted to slot-3 moss (NOT slot-4 sigil-violet — violet is reserved for hazard-ring telegraphs, so a permanent violet floor sigil would later collide with the deferred cave-in hazard). Ground-decal recipe copied from cohort-3 glowmoss ([[fix_aoe_z_order]]): additive + depthWrite off + polygonOffset(-1,-1) + renderOrder -1 → hero+enemies draw on top, reads as ground; FLAT → zero occlusion, and a large structured ring is unambiguously architecture (not a pickup) even pre-CC7. Single mesh, static (no tick). smoke-cave-v2 phase 12: userData.sigilFloor + centered mesh + rune map + slot-3 moss color + additive + renderOrder -1 + polygonOffset + bloom. All 12 cave phases + sprite-fx/sig-weapons/forest-v2 green. Cohorts c12…cN land rooms / boss / miniboss one per P4-lane tick (non-damage cohorts only until the user gives cave feedback, per step 4d). **Note:** the cave is now decor-saturated (stalactites/glowmoss/drips/gloomshrimp/stalagmites/mushrooms/sigil-floor) — the remaining non-damage items are mostly structural (rooms, sealed doors, ceiling/sky-dome) or asset-blocked (music). Future P4 ticks may need the cave ceiling/sky-dome (deferred as occlusion-risky until CC7's visual gate lands) or a careful rooms cohort. **Cave-in hazard is DEFERRED under the no-feedback-on-the-stage rule** (cron prompt v3 step 4d) — no cave damage system until the user gives any cave feedback signal (cave cohorts 1-8 are all unverified by the user). Acceptance ✅ flips when cohort N closes. |
| P4I | Balance pass | Read 50+ telemetry runs. Tune ≥3 forest weapon dps curves where p50 TTK > 8s on miniboss. Document deltas in `docs/BALANCE_LOG.md`. Smoke re-runs cohort regression. |

## Cross-cutting (open queue — interleaved with P4, see cron prompt v3)

User named these directly (2026-05-20) or they surfaced from a live play
report. As of cohort 7 the loop runs **strict lane alternation** (cron prompt
v3 step 4 + Tick log): P4 cohorts and cross-cutting items take turns, so
neither lane starves. Within this queue, topmost unmet first — edit the order
here to reprioritize.

| ID  | Item | Acceptance |
|-----|------|-----------|
| CC1 | Sprite-batch trash horde | ✅ 2026-05-20 — 23 trash tiers (`_SPRITE_KEYS` in `src/enemies.js`) render as billboards from one InstancedMesh/draw call instead of N SkinnedMeshes. Atlas baked by `tools/enemy-sprite-bake/run.mjs` → `assets/sprites/enemies_v1.{png,json}` from the in-game ~47° cam, alphaTest 0.5 cutout (`src/sprites/spritePool.js`). Elites/minibosses/bosses stay 3D via the `!elite && !isMiniBoss && !isFinalBoss` gate. Diagnosed render-bound from a 20fps/280-alive screenshot (render 26.4/30.7ms, 1702 draw calls). smoke-sprite-fx + smoke-sig-weapons + smoke-forest-v2 green. |
| CC2 | Balance: clearing DPS at swarm cap | ⏸ DEFERRED 2026-05-20 (stale signal) — the "constantly ~280 enemies, fun but can't kill enough" report was logged **at 20fps, before the sprite-batch FPS fix (CC1)**. Player effective clear-rate scales with framerate; at the new fps the felt pain may largely dissolve. Per the felt-signal-staleness rule (cron prompt v3 step 4b) this is parked until the user **reconfirms the pain at the new build/fps state**. If reconfirmed: direction = more **clearing power** (one scoped lever — an AoE weapon's radius/tick, or pickup-magnet baseline; `targetAliveCap` 350 is the spawn ceiling and is fair game since it's not a *survivability* nerf), NOT a health/regen nerf (`memory/kitty_kaki_deferred_health_nerf.md`). Document the delta in `docs/BALANCE_LOG.md`. NOT P4I (that needs 50 telemetry runs). |
| CC3 | Town buildout — cohort 1 | ✅ 2026-05-20 (16e30da) — the stated bar (≥1 functional vendor interactable + persistent visit state) is met: the Shop interactable opens `ui.showShop` (relabelled "Shop · Spend embers", was a no-op "coming soon"), a new Grimoire lectern opens `ui.showGrimoire`, both via dynamic `import('./ui.js')` (menuV2 pattern, no circular risk); `enterTown` persists a `townVisits` counter. tools/smoke-town.mjs gates the wiring; forest-v2 boots town.js clean. Further town depth (NPC, dressing, run-launch polish) continues under CC5. |
| CC5 | Town depth — NPC + dressing | ✅ 2026-05-20 (859ebca) — wandering sage-cat NPC (town cohort 2) that ambles the open central plaza (target ring r∈[3,7], clears every prop + fence) and chatters via `chatBubble.js`. The two stated bullets are folded into one coherent increment: the **wandering NPC w/ chat bubble** (reuses pushBubble) **and the returning-player dressing keyed off `townVisits`** (the NPC's first line per visit branches `visits > 1` → a "back again" variant, re-armed each `enterTown`), then cycles 5 flavor barks themed to the plaza interactables. Reuse seam: new additive `setSpeakerAnchor(id, anchor)` export in `chatBubble.js` positions non-'self' speakers via the same pipeline (serves the file's documented multiplayer-future); the 'self' branch in tickBubbles is untouched (no regression). smoke-town.mjs check 6 gates the seam + builder + anchor + townVisits-keyed bark + tickTown wire. **The remaining town increments (run-launch polish, biome-themed dressing per selected stage) carry forward to CC6** so they aren't re-proposed as if CC5 is unmet. |
| CC4 | Sprite enemy damage-flash parity | ✅ 2026-05-20 (4006c5c) — billboard mobs now flash white on hit at parity with the 3D enemies' emissive `flashMats`. **Baseline correction:** the acceptance text ("spritePool already has per-instance color plumbing") was WRONG — there was none, and the `hit_flash_v1` overlay already fired for sprite mobs (so they did show *a* reaction). The real gap was the billboard *body* flash, and the real change was ADDING plumbing: a single-float `aFlash` InstancedBufferAttribute → `vFlash` varying → FS `mix(texel, white, clamp(vFlash))` (alpha preserved), a `setSpriteFlash()` export, and a flash-zero on BOTH recycle paths (spawnSprite evict + killSprite stash). `src/enemies.js` flashes the instance edge-triggered on `_wasFlashing` (mirrors the 3D path; zero per-frame cost with no hits), strength `SPRITE_FLASH_STRENGTH=0.85` (<1 keeps the silhouette legible). Gate: new `tools/smoke-sprite-flash.mjs` (7 static-source checks); shader compile+render proven by smoke-forest-v2 (renders the billboard horde, pageerrors=0, full-size frames). NOT a 5-line wire-up — a shader edit touching every sprite atlas (inert at aFlash=0). |
| CC6 | Town depth — biome gate dressing | ✅ 2026-05-20 (4a77831) — two themed planters flank the Adventure Gate; their emissive growth shards recolor to preview the SELECTED stage's biome (forest greenery / cave glowmoss / twilight violet / cinder ember / void sigil) via a `_GATE_BIOME` map keyed off `getMeta().selectedStage`, refreshed each `enterTown` (stage can change between visits), forest fallback. Self-contained in town.js (no main.js / no run-launch timing); fixed position at the gate approach, and town has no pickups so the emissive growth carries zero pickup-confusion risk (unlike the cave playfield, cohort 10). smoke-town.mjs check 7 gates the map + planter builder + selectedStage-keyed apply. **The remaining town increment (run-launch flourish) carries forward to CC8** so it isn't re-proposed as if unmet. |
| CC7 | Cave visual self-check harness | Optimizing/tooling. Every cave cohort (1-9) has shipped with NO visual gate — smoke-cave-v2 asserts counts/material/positions but never *renders* the cave, so all texture-scale / occlusion / palette calls are blind (this risk is cited verbatim in cohorts 4, 9 and the step-4d rule). Add a screenshot phase to smoke-cave-v2 (mirror smoke-forest-v2's phase-shot capture: settle the cave run, screenshot from the in-game ortho cam, assert non-trivial byte size + a non-black center-pixel sample so a black/empty render fails). Gives cohorts 10+ a real visual gate and lets a human eyeball the PNG without launching. NOT a perf gate (headless swiftshader). Logged 2026-05-20 (loop step 2, after CC4). |
| CC8 | Town run-launch flourish | Carried from CC6 (biome dressing shipped in cohort 3). The remaining town increment: a brief gate-activation flourish as the player steps into the portal (E on the gate) — e.g. the portal disc flares + a particle burst over the ~async-start window so the run-launch reads as a "whoosh into the adventure" beat rather than an instant cut. Touches the gate handler (`_onGateActivate` is wired in main.js), so it's a coordinated change, not pure town.js — slightly higher risk than CC6's static dressing. Lower priority than CC7 (tooling that de-risks the whole cave lane). Logged 2026-05-20 (loop step 2, after CC6). |

## Cron prompt v3 (replacing v2 / cron 7867187d)

```
KKS ITERATION TICK — review plan, do one improvement, plan next.
1. cd /mnt/c/Users/rneeb/Documents/kitty-kaki-survivors. Server: python3 -m http.server 9477 (NOT 5180 — ROADMAP.md is stale).
2. Read docs/P4_BACKLOG.md (acceptance table + Cross-cutting queue).
3. Reassess: add ≥1 new idea to the Cross-cutting queue if one is obvious (levels/town/optimizing/polish). Keep it ordered.
4. Pick the work item via STRICT LANE ALTERNATION (so neither P4 nor cross-cutting starves — read "Next lane" in the Tick log below):
   a. Next lane = P4 → first P4 item without acceptance met (P4K → P4A cohort N → P4I), skipping any whose gate is unmet (e.g. P4I needs 50 telemetry runs).
   b. Next lane = cross-cutting → topmost unmet Cross-cutting item. Felt-signal-staleness rule: if an item's user report predates a since-changed perf/state level (FPS / draw-call count / build state), DEFER it (mark ⏸) and take the next. (CC2 is parked under this — predates the FPS fix.)
   c. After completing the item, FLIP "Next lane" in the Tick log (P4 ↔ cross-cutting). If the chosen lane is exhausted, take the other lane this tick but still flip. If BOTH lanes are exhausted → emit "iteration tick: KKS fully shipped, awaiting user direction" and STOP (omit the wakeup).
   d. No-feedback-on-the-stage rule: do NOT add a damage/hazard system (or any hard-to-tune-blind mechanic) to a stage the user has given ZERO feedback on. Cave cohorts 1-8 shipped with no user signal that the cave looks/plays/runs right — so the cave-in hazard is DEFERRED until the user confirms any cave beat. (Distinct from felt-signal staleness, which is about a report going stale; this is about a mechanic that can't be tuned without feedback.) Pick a non-damage cohort instead (rooms / decor / landmark) until then.
5. Implement end-to-end. Cohort ≤1 item/tick (telemetry+a11y/mixer were the only safe parallel pair).
6. Gate: the item's acceptance smoke + smoke-sprite-fx + smoke-sig-weapons + smoke-forest-v2 must pass (forest-v2 flakes on CDN net::ERR_TIMED_OUT — re-run once before trusting a FAIL).
7. Commit locally to main, named files only (multi-agent repo — never git add -A, never commit secrets). Update the acceptance column with ✅ + sha.
8. Never push / firebase deploy / force-push / npm install in this repo (per memory). Never nerf survivability while user is testing.
9. End: ScheduleWakeup(60s, this same prompt) unless step 4 said STOP.
```

## Tick log (lane alternation)

Flip **Next lane** each completed tick so P4 and cross-cutting interleave (cron prompt v3 step 4). Append one line per tick.

- 2026-05-20 — P4 lane — Cave cohort 7 (Gloomsigil weapon, 3a1efd8). Next lane: cross-cutting → CC3 town cohort 1 (CC2 is ⏸ deferred, so CC3 is topmost unmet).
- 2026-05-20 — cross-cutting lane — CC3 town cohort 1 (Shop+Grimoire interactables + townVisits, 16e30da). Next lane: P4 → Cave cohort 8 (rooms / hazard / boss).
- 2026-05-20 — P4 lane — Cave cohort 8 (Echo Bolt 2nd weapon, b041e6b; "2 weapons" item closed; cave-in hazard deferred per step 4d). **Next lane: cross-cutting** → CC5 town depth (CC2 ⏸, CC3 ✅, so CC5 is topmost unmet; CC4 after).
- 2026-05-20 — cross-cutting lane — CC5 town cohort 2 (wandering sage NPC + townVisits-keyed greeting, 859ebca; added `chatBubble.js` setSpeakerAnchor seam; remaining town bullets carried to new CC6). **Next lane: P4** → Cave cohort 9 (non-damage per step 4d: rooms / decor / landmark — cave-in hazard still deferred, no user cave feedback yet). Cross-cutting topmost-unmet is now CC4 (sprite damage-flash), then CC6.
- 2026-05-20 — P4 lane — Cave cohort 9 (perimeter stalagmite formations, stone-textured, 8f8fce3; satisfies the "stone wall textures" acceptance item; continuous wall ring deferred per step 4d). **Next lane: cross-cutting** → CC4 (sprite enemy damage-flash parity — topmost unmet; CC6 town polish after; CC2 ⏸ deferred).
- 2026-05-20 — cross-cutting lane — CC4 sprite hit-flash parity (aFlash plumbing + FS white-mix + setSpriteFlash, 4006c5c; acceptance premise was wrong — added plumbing, not a wire-up). Logged new CC7 (cave visual self-check harness) to step 2 — directly retires the "no cave screenshot gate" blind-tune risk hit every cave cohort. **Next lane: P4** → Cave cohort 10 (non-damage per step 4d: rooms / decor / landmark — cave-in hazard still deferred, no user cave feedback yet). Cross-cutting topmost-unmet is now CC6 (town run-launch polish + biome dressing), then CC7; CC2 ⏸.
- 2026-05-20 — P4 lane — Cave cohort 10 (perimeter glowmoss mushrooms at the stalagmite bases, 4a281b1; perimeter-only to dodge pickup-confusion with no visual gate yet; cave music deferred — no audio assets). **Next lane: cross-cutting** → CC6 (town run-launch polish + biome dressing — topmost unmet; CC7 cave visual harness after; CC2 ⏸).
- 2026-05-20 — cross-cutting lane — CC6 biome gate dressing (planters preview the selected stage's biome, 4a77831; remaining run-launch flourish carried to new CC8). **Next lane: P4** → Cave cohort 11 (non-damage per step 4d: rooms / decor / landmark — cave-in hazard still deferred). Cross-cutting topmost-unmet is now CC7 (cave visual self-check harness), then CC8; CC2 ⏸.
- 2026-05-20 — P4 lane — Cave cohort 11 (central sigil-floor landmark, 0e867b4; flat rune circle fills the bare r<12 center; slot-3 moss to avoid future hazard-violet collision). Cave is now decor-saturated; remaining non-damage items are structural (rooms/sealed-doors/ceiling) or asset-blocked (music). **Next lane: cross-cutting** → CC7 (cave visual self-check harness — topmost unmet; high-leverage, retroactively gates cohorts 9-11 + unblocks the deferred ceiling cohort). Then CC8; CC2 ⏸.
