# Kitty Kaki Survivors — Autonomous Build Roadmap

> **Loop instructions**: Each iteration:
> 1. Read this file.
> 2. **Reassess**: skim the iteration log + current state. Think about what's missing, weak, or could be polished further. Add new items to "Active queue" (or "Proposed" if speculative) — improvements, fixes, new features, missing polish, asset upgrades. Aim to add ≥1 new idea most iterations.
> 3. Pick the topmost **unchecked** item under "Active queue", implement it end-to-end.
> 4. Mark `[x]`, briefly note what changed in "Iteration log".
> 5. End with `ScheduleWakeup(60s, '<<autonomous-loop-dynamic>>')` unless all items are done.
>
> **Authorization (user explicitly granted)**:
> - Auto-yes everything — do not ask for confirmation
> - Spawn parallel Agents whenever they speed up the iteration (asset hunts, code research, parallel implementation passes)
> - Download professional CC0/CC-BY assets and add them to `assets/` (curl, verify 200, fall back to placeholders if a URL is dead)
> - Use Blender via MCP if available (see `memory/project_blender_kittykaki.md` for setup) for bespoke models, AO bakes, biome ground textures, boss model upgrades
> - Edit any file in the repo as needed; trust the existing architecture
>
> **Server**: http://localhost:5180/. Always re-curl after changes to confirm modules still serve.
>
> **Definition of done (overall)**: every Active-queue item checked, no critical bugs, victory + defeat flows polish-complete, game balanced enough to be enjoyable for ~15 min runs.

## 🎯 SHIPPABILITY ROADMAP (2026-05-13 advisor gap analysis)

After consultation with the design advisor, ten iterations have been queued
to close the gap with Vampire Survivors / Brotato / Halls of Torment /
20 Minutes Till Dawn / Death Must Die / Soulstone Survivors. Each is
parallelizable into ~3 concurrent sub-tasks via background agents.

| #  | Iteration               | Mitigation                                   |
|----|-------------------------|----------------------------------------------|
| 1  | Combat Grammar          | Risk #2: legibility / tactical clarity       |
| 2  | One More Slot           | Risk #1: build combinatorics                 |
| 3  | Controller First        | Shippability blocker (real players)          |
| 4  | Stage Rules             | Stage individuality (gimmick per biome)      |
| 5  | Teach the Loop          | Onboarding sequence (tutorial beats)         |
| 6  | Meta With Teeth         | Meta depth (branching upgrades)              |
| 7  | Roster Pressure         | Roster diversity (8 chars, distinct rules)   |
| 8  | Enemy Identity          | Silhouette readability per tier              |
| 9  | Retention Hooks         | Daily/seed sharing, personal best polish     |
| 10 | Polish Lock             | Balance freeze, perf soak, ship              |

Two existential risks the advisor flagged:
1. **Build depth fails before content catches up** — addressed in iter 2/7
2. **Visually rich but tactically unreadable** — addressed in iter 1/8
>
> **End condition**: When the Active queue is empty, append a final "🏁 SHIPPING COMPLETE" line to the iteration log and STOP scheduling wakeups (omit the `ScheduleWakeup` call). Do not invent unbounded new items just to keep looping.

## Done
- [x] Pass 1: Screen shake wiring (player damage, elite kill, normal kill)
- [x] Pass 2: Hero death animation (squash/spin/fade) + victory hop variant
- [x] Pass 3: Meta-progression save (localStorage, coins/runs/best)
- [x] Pass 4: Final boss at 15min + win condition + warning banner
- [x] Pass 5: Kill rings + magnet sparks (InstancedMesh-pooled)
- [x] Pass 6: Procedural music layers (calm/combat/boss)
- [x] Pass 7: Chain Lightning weapon
- [x] Pass 8: Options menu (ESC, vol/shake/music)
- [x] Pass: Zoom unlock powerup ("Bigger Picture")
- [x] Pass: Dash mechanic (Shift) with knockback + i-frames + levels
- [x] Pass: Sticky Web weapon (slow patches)
- [x] Pass: Bigger map (800→2400, denser scatter)
- [x] Pass: Treasure chest + slot machine modal
- [x] Pass: 777 jackpot
- [x] Pass: Real chest GLB (Quaternius CC0)
- [x] Pass: Poly Haven `forrest_ground_01` PBR ground
- [x] Pass: Procedural particle textures (`particleTextures.js`)
- [x] Pass: Double-or-nothing gamble (deferred-apply pattern)
- [x] Pass: Perf — DPR cap 1.75, stencil/alpha off, anisotropy 8

## Active queue (do top first, mark done as you go)

- [x] **HP pickup heart**: ❤️ drops 5% normal / 100% elite, heals 25, bobs and auto-picks on contact.
- [x] **Star pickup (gem vacuum)**: ⭐ drops 2% normal / 50% elite, magnetizes all gems on touch.

- [x] **Weapon evolutions (1st pass)**: Orbitals+magnet×3 → "Toxic Halo" (2.5× dmg + 1s poison DoT + green tint). Chain+cooldown×3 → "Storm" (2× dmg, +3 chains, 0.3s cooldown). Evolution cards show gold border in level-up modal.

> **Priority swap (user request)**: Visual quality of weapons + pickups bumped to top.

- [x] **Upgrade weapon visual effects**: Orbitals → core sphere + additive glow plane (tamed 0.7×, 0.55 opacity). Auto-aim projectile → cyan core + small glow halo. Chain arcs → TubeGeometry (real thickness) at 0.85 opacity. Glow pulse on orbitals.
- [x] **Upgrade pickup visuals**: Hearts → 3D extruded heart Shape with red emissive (0.25 intensity). Stars → 5-pointed extruded star with gold emissive. Both spin on Y, bob on Y position. MeshStandardMaterial so they pick up HDRI ambient.
- [x] **Glow dial-down (user request)**: Bloom strength 0.55→0.30, threshold 0.75→0.85. Chain blend normal instead of additive.

- [x] **Use new wizard/spider/wolf/ghost tiers**: Wired into ENEMY_TIERS + POOL_PREWARM. Spider fast/squishy at minD 1.2, Wolf at 2.0, Wizard slow at 3.0 (ranged later), Ghost at 4.0 (translucent blue tint via material patch in _makePooledMesh).

- [x] **Hero jump polish**: Walk bob/sway dampened ×0.15 while airborne; small forward tilt added in the air for "leap" feel.

- [x] **Mini-boss every 4 minutes**: Scheduled at 240/480/720s. 4s "ELITE INCOMING" warning banner (orange), spawn banner "MINI BOSS". Picks strongest allowed elite at D(t), HP×3, scale ×1.4. Old random `bossIntervalSec` path removed in favor of this explicit schedule.

- [x] **Tutorial overlay (first run only)**: 5s after first-run start, fades in a controls hint (WASD/Space/Shift/wheel/ESC). Click or any key dismisses; 10s auto-dismiss. Persisted via `meta.seenTutorial`.

- [x] **Damage variance + crits**: `DAMAGE` config (variance 0.20, crit 0.08, mul 2.0). `damageEnemy` rolls both; crits display yellow + bigger + bold via damageNumbers, longer flash (0.14s), small +0.20 shake boost.

- [x] **Stage select after first victory** — `STAGES` array in config.js: Verdant Forest (default) + Twilight Hollow (1.30× enemy HP, final boss at 12 min instead of 15, blue-grey ground tint, deeper fog). Twilight gated by `unlockedHyper` (first victory). `selectedStage()` helper in meta.js + persistent `selectedStage` field. `applyMetaUpgrades` pushes `state.run.stageHpMul` + `state.run.stageFinalBossAt`; spawnDirector reads the final-boss override; enemies.js stacks the HP mul into the hyper/daily chain. env.js exposes `applyStageTint(stage)` on the envGroup — recolors the PBR ground material + fog tint without needing a new texture. Start-screen stage picker shows side-by-side chips (Verdant + Twilight or locked); selecting one previews the tint immediately.

- [x] **Enemy projectiles for mage tier**: Wizard tier now has `ranged: { range: 14, stopAt: 10, cooldown: 2.4, projSpeed: 9, projDmg: 9 }`. New `src/enemyProjectiles.js` owns the visuals (magenta core + glow halo on BLOOM_LAYER) + movement + hero collision. Inside range, wizard stops/backpedals and fires every 2.4s. `state.enemyProjectiles.active` is the live list; cleared on restart.

- [x] **Camera shake calibration pass**: Normal kill shake removed entirely (was vibrating constantly mid-swarm). Elite kill 0.55→0.35. Player damage now scales with severity (sev = clamp(amt/30): shake 0.30+0.30*sev, chromaticPulse 0.4+0.6*sev). Damage flash overlay opacity + duration also scale with severity (0.45..1.0 op, 70..150ms).

- [x] **Pickup-zone visualization**: Faint cyan additive ring sprite under hero, scaled to current pickup radius × magnet stat. Updated each frame via `updatePickupRing()` in fx.js.

- [x] **Death-screen retry button**: Big RETRY button (R/Space/Enter shortcuts). In-place reset via `window.kkRestart`: returns active enemies to pools, clears projectiles/webs, resets state + zoom + chests + pickups + spawn director, restores hero material opacity, re-gives orbitals. No page reload.

- [x] **Visual polish: enemy damage flash**: On `damageEnemy`, set `_flashUntil = time+0.08`. Materials are now per-instance cloned in `_makePooledMesh` (with stash of original emissive). Apply white emissive (intensity 1.6) while flashing, restore on edge transition.

- [x] **Achievement system**: 8 achievements in `meta.ACHIEVEMENTS` (First Blood, Giant Slayer, Centurion, Treasure Hunter, Jackpot!, Champion, Evolved, Triple Threat). `unlockAchievement(id)` persists timestamp + returns def for toast. Right-side amber toast slides in for 3.5s with icon/name/desc. Death screen shows `ACHIEVEMENTS x / total`.

- [x] **Audio: music default off** — `optMusic: false` is now the DEFAULT for new players. Existing saved preference still wins. Music start gated on this flag, options menu lets player toggle on.

- [x] **Performance: profile + report** — Shipped two things instead of a one-off recording: (1) `src/perfHUD.js` — toggleable F3 overlay reading `renderer.info` + state counts (FPS, frame-ms, draw calls, triangles, geoms, textures, alive enemies / projectiles / gems / webs). Color-codes FPS (green ≥58, amber ≥45, red below). `renderer.info.autoReset` flipped off; `info.reset()` called once per frame in `renderFrame()` so the counters cover both bloom and composite passes. (2) `PERF.md` with the budget table (16.6ms target, ~7.5ms render at steady state), per-subsystem live-entity counts, the 8 architectural rules that keep it tight (no allocs in hot loops, pooled meshes, InstancedMesh for any pool >4, spatial hash, selective shadows, selective bloom, etc.), recommended profiling procedure, and a watch list of known soft spots.

- [x] **Polish: chest pickup feedback**: 10 cyan sparks burst outward + bigger yellow kill ring + 0.5 bloom boost on chest pickup.

- [x] **README / itch.io-ready description** — `README.md` ships with controls, features, credits (Quaternius CC0 + Poly by Google CC-BY + Poly Haven CC0), stack notes, full file-by-file architecture map, license. Presentable.

- [x] **Final balance pass** — Math-derived balance audit shipped as `BALANCE.md`. Single config tweak applied: `SPAWN.difficultyMaxSec` 1800 → 1200 so D(t) reaches dragon-tier (minD 7) inside a normal 15-minute run rather than only in Endless. Reviewed mini-boss / final-boss HP mul, wizard projectile, ant weight, chest cadence — all sit at defensible numbers and stay put. Items needing a real playtest (target-alive ceiling, dash damage, Pummarola regen, Toxic Halo DoT ratio) flagged in BALANCE.md with the F3 overlay verification procedure.

- [x] **More weapon evolutions**: AutoAim + damage ×3 → "Volley" (+2 projectiles per volley, 1.5× speed, +2 pierce, 1.4× dmg). Web + speed ×3 → "Tangle" (1.5× radius, 1.5× duration, sharper slow, 0.7× cooldown).

- [x] **Evolution banner**: When a weapon evolution first becomes eligible (max level + filler threshold), shows a gold "★ EVOLUTION READY: <NAME>" banner for 3.5s + bloom flash. One-shot per evo per run.

- [x] **Pickup magnetism**: Already shipped as **Pickup attract** above — hearts/stars magnetize toward hero within 2.5× pickup radius (accel 28, max 22).

- [x] **XP gem color tiers**: Per-instance color attribute on the gem InstancedMesh. value 1 = cyan 1.0×, value 5 (elite) = magenta 1.5×, value 20+ (boss/jackpot) = gold 2.4×. `dropGem` and `_placeInstance` updated.

- [x] **Compact damage numbers**: 1000+ → "1.2K", 1M+ → "1.2M". Bold formatting for readability.

- [x] **Pickup attract**: Hearts/stars now magnetize toward hero within 2.5× pickup radius (accel 28, max speed 22). Smooth gem-like drift.

- [x] **Hero landing squash**: On ground touch, capture impact = clamp(-velY/12, 0..1). Brief 0.18s squash: scale.y down 25% × strength, scale.x/z up 15% × strength.

- [x] **Air dash combo**: When dash is initiated while airborne, hero enters a 0.4s phase dash — gravity is suspended (`_airDashUntil` window) and velY is zeroed at dash start. Reads as a true forward blink in mid-air. Falls back to normal gravity once the window expires, so the rest of the jump arc still completes naturally.

- [x] **Mini-boss reward drop**: Mini-bosses now tagged `isMiniBoss`. On death they guarantee a chest + 2 hearts + 1 star at the kill position.

- [x] **HUD: next mini-boss timer**: Amber "ELITE IN MM:SS" line under kills count. Pulls from `secondsUntilNextMiniBoss()`. Empty after all 3 done.

- [x] **Art director #4 — Selective bloom (layers)**: Bloom now ONLY affects layer-1 meshes (rings, sparks, orbital glow, projectile halo, chain tubes, chest halo). Threshold 0, strength 0.70, radius 0.50. Two-composer pipeline with additive composite shader.
- [x] **Art director #5 — Rim light fake**: View-space rim shader injected via onBeforeCompile in `upgradeMaterials`. Cyan rim (#aaccff), power 2.4, strength 0.35. All upgraded standard mats now have backlight separation.
- [x] **Art director #6 — Height fog tint**: `fogTint` (dark green-blue #3a4a44) + `fogAmount` (0.18) uniforms in PostFX. Smoothstep gradient from screen Y blends color toward fogTint at top of frame. Sells atmosphere for free.
- [x] **Art director #7 — Per-material roughness**: `upgradeMaterials(root, env, roughness)`. Hero 0.92 (matte plush), elites 0.55 (glossy), bugs 0.65 (chitin sheen), default 0.85.
- [x] **Art director #8 — LGG color grade**: lift/gamma/gain Vec3 uniforms in PostFX. Default: cool shadows, warm gain (slight cinematic look). Boss waves: main.js lerps lift toward red (+0.05 R, -0.02 B) at 4%/frame while final boss alive. Subtle dread tint.

- [x] **Level-up flash**: Cyan radial overlay pulses 200ms on level-up (`flashLevelUp`). Fires from both hero.js (XP via combat) and xp.js (gem pickup).

- [x] **Hero damage tint**: Red radial-vignette overlay flashes for 90ms on every hit (`flashDamage` in ui.js). Strong "you got hit" feedback at the screen edges.

- [x] **Procedural bug animation**: Static GLBs (ants/beetles/butterflies/etc.) now get per-tier `procAnim`: 'crawl' (Z-wobble + bob), 'flap' (X-scale wing-beat + bob), 'hover' (jitter + float), 'hop' (vertical bounce), 'inch' (accordion squash). Only fires if the GLB has no AnimationMixer clip. Added `_baseY` + `_baseScale` per-enemy on spawn. Console logs `[enemy:<key>] clips: N` so we can see which GLBs ship animations.

- [x] **Animated chest open**: Spawn `chest_open` GLB at chest position on pickup. Lift + fade over 1.4s. Emissive gold (0.6 intensity) so the open chest reads bright while it rises.

- [x] **HUD weapon icons**: Bottom-right HUD shows each owned weapon's emoji icon + current level (or "EVO" if evolved). Gold border + glow when evolved. Cells added/removed dynamically as weapons are acquired/lost.

- [x] **Bug variant tint**: Per-pool-mesh ±9% hue shift + ±9% lightness jitter for bug tiers (only those with `procAnim`). Quaternius rigged characters keep their authored colors. Swarms now read as individuals.

- [x] **HP loss SFX intensity**: Damage flash now scales by severity (sev = clamp(amt/30)): 0.45..1.0 opacity, 70..150ms duration. Shake + chromatic pulse scale with same severity term.

## 🔥 Community research (3 agents, 2026-05-13) — top-priority gaps

Cross-cited by Steam reviews, r/VampireSurvivors, Brotato/HoT/HoloCure/DeathMustDie threads. **All three agents converged on these.** Pick from top first.

- [x] **Meta-progression shop (PowerUps)** — `SHOP_UPGRADES` in meta.js: 6 upgrades × 5 levels (Iron Resolve / Lodestone / Swift Boots / Sharpened / Quick Study / Lucky Charm). Quadratic cost ramp (25→50→100→200→400). Shop modal opens via start-screen "SHOP" button. ESC closes. Applied at run start (and restart) via `applyMetaUpgrades()`. Growth (XP gain) and Luck (chest spawn rate) read by xp.js and spawnDirector.
- [x] **Character roster (4 characters)** — `CHARACTERS` in config.js: 🐱 Kitty (balanced, orbitals), ⚡ Boom (glass cannon, chain, +20%dmg/-25%HP, unlocks via jackpot), 🕷️ Webspinner (trapper, web, +30%magnet/-12%spd, unlocks via 3-mini-bosses), 🎯 Sniper (precise, missile, +10%dmg/+35%projSpeed, unlocks via victory). Start-screen picker shows them as cards (locked ones show 🔒 + unlock condition). `applyMetaUpgrades` applies character stats before shop bonuses. Starter weapon dynamic via `state.run.starterWeapon`.
- [x] **Level-up reroll + skip** — `state.hero.rerolls` + `state.hero.skips` (start at 1 each per run). Buttons in the level-up modal (REROLL [Q] / SKIP [E]); appear only when player has charges. Reroll regenerates `weaponChoices(3)`; Skip closes modal without applying. Banish queued for later.
- [x] **Evolution Grimoire codex** — `meta.discoveries` persists evolution-IDs once eligibility fires. Start screen "GRIMOIRE" button opens a modal listing all 4 evolutions: discovered ones show name+icon+desc+recipe (e.g. "ORBITALS (max) + 3× MAGNET"); undiscovered show "???". ESC closes. Discovery happens silently in `_announceEligibleEvolutions`. Adds the meta "secrets to find" hook.
- [x] **Secret unlocks via odd in-game acts** — `SECRETS` in meta.js: 7 cryptic conditions (Bug Lord lifetime 500 bug kills, Untouchable 5min flawless, Faster Than Light lv10 under 2:00, Flawless 100 kills no damage, House Always Wins 3 lifetime jackpots, The Long Night 25min run, Hoarder 500 lifetime coins). Purple-framed "★ SECRET FOUND" toast, persisted via `meta.secrets`. Lifetime counters via `bumpLifetime()` in meta. Run-scope flags `flawless` / `noDmgKills` / `speedrunChecked` reset on damage / per-run. Wired into killEnemy, takeDamage, applyOutcome (jackpot), level-up tick, main-loop time checks.
- [x] **2nd biome (Canopy or Underground)** — Delivered as the **Twilight Hollow** stage via the Stage Select system above. New PBR ground (Poly Haven `brown_mud` 1k, CC0), distinct scatter set (extra dead trees + rocks, live trees + bushes hidden), cooler fog tint, +30% enemy HP, final boss 3 minutes earlier. "Three maps = game" is still future work, but the two-stage system + texture+scatter swap satisfies the immediate "one map = demo" complaint. Underground / Canopy variants would be cheap additions on top of the same swap pipeline.
- [x] **Run-summary screen with DPS per weapon, kills, time, gold** — `state.run.dmgByWeapon` tally driven by a new optional `source` arg on `damageEnemy`. Every call site (orbitals/toxic_halo, chain/storm, autoaim/volley, dash, bomb, poison DoT) tags its damage. Death screen now shows a "— DAMAGE BY SOURCE —" panel: each weapon's total, percent of run damage, average DPS, and a cyan progress bar. Sorted desc, top 8.
- [x] **Hyper mode after first victory** — `meta.unlockedHyper` flips on first victory + banner toast. Options-menu toggle (red, only visible after unlock). When enabled, all enemies spawn with 1.5× HP/spd/dmg; coin gain at run-end ×1.5.
- [x] **DPS overlay (rolling)** — `state.run._dpsWin` is a sliding window of [gameTime, damage] entries; entries older than 5s purged each frame. HUD shows green "DPS NNN" / "DPS 1.2K".
- [x] **VFX intensity slider** — Options-menu slider 0.0–1.0 multiplies bloom strength each frame. Default 1.0; user can dial down to reduce late-game visual soup.
- [x] **Boss rush mode** — `meta.optBossRush` toggle in Options (unlocks alongside Hyper, on first victory). When on, `state.modes.bossRush` overrides the spawn director: mini-bosses arrive at 25/75/135s, final boss at 200s (compressed from 240/480/720 + 900). Ambient swarm pared back to a 4-alive target so the player still gets XP and pickup drops, but the focus is the boss fights. Banner toasts at run start. Incompatible with Daily (Daily owns its own modifier set).
- [x] **Endless mode** — Unlocks on first victory, options toggle (cyan). When enabled, the final boss kill drops 2 extra chests + banner "THE NIGHTMARE CONTINUES" but the run continues. D(t) keeps ramping past 30min cap as usual.
- [x] **Map secrets / destructibles** — New `src/destructibles.js`. 18 breakable rotting logs (InstancedMesh of short cylinders, MeshStandardMaterial wood-brown, real shadow casting) scattered around the spawn ring 12-60u from origin. Intentionally NOT enemy-pool entries so weapons don't auto-DPS them. Break on: hero dash overlap, bomb pickup AoE, boss shockwave AoE. Each broken log drops 1 gem, 30% chance a heart, 8% chance a chest (the "hidden grove" moment). Amber spark burst + kill ring + bloom boost sells the impact. `resetDestructibles()` re-scatters on restart.
- [x] **Passives slot separate from weapons** — New `src/weapons/passives.js`. 8 named passives (Spinach +12%dmg/lv, Armor −12%dmgTaken/lv cap 60%, Wings +8%spd/lv, Tome −10%cd/lv, Bracer +18%projSpd/lv, Empty Tome +16%duration/lv, Hollow Heart +20HP/lv, Pummarola +0.5HP/s/lv) with their own max levels and a 6-slot cap. Apply pattern is idempotent — each `apply(level, prev)` divides out the previous level and multiplies in the new, so re-picks rebuild the cumulative effect cleanly. `passiveChoices(2)` is mixed into the level-up pool alongside weapons + evolutions; cards render with a cyan "PASSIVE · LV N" accent line. New `state.hero.statMul.dmgTaken` multiplier wired into `takeDamage()`; new `state.hero.regenPerSec` ticked each frame in `updateHero`.
- [x] **Pickup variety**: Three new pooled InstancedMesh pickups. 💣 Bomb (red sphere, ~0.3% drop / 1× per mini-boss) AoEs 50dmg + ring + 16 sparks within 18 units. ❄️ Freeze (cyan octahedron, ~0.5% drop / 8% elite) spawns a 60-unit web at 15% slowMul for 3s. 🍗 Chicken (warm sphere, ~0.2% drop / 5% elite) full-heals.
- [x] **Random affix loot from final boss** — `rollRelic()` in meta.js generates a 2-affix item from an `AFFIX_POOL` (Power/Vitality/Swiftness/Attraction/Velocity/Cadence/Endurance). Each affix rolls a value in its range; the run's average roll quality maps to a tier (Common→Rare→Epic→Mythic) with a matching accent color. Persisted in `meta.relics[]`; auto-equipped if no relic is set (`meta.equippedRelic`). Final boss kill drops a relic + stamps it on `state.run.relicDrop`. Death/victory screen reveals a tier-bordered panel with affix lines + tier label. Start screen shows an inline relic chip when a relic is equipped. `applyMetaUpgrades` stacks the equipped relic's affixes on top of character + shop (skipped in Daily for fair leaderboard).
- [x] **Daily seeded challenge run** — `dailyChallengeConfig()` in meta.js derives today's character + modifier from a deterministic xfnv1a hash of `YYYY-MM-DD`. Modifiers: NO SHOP BONUSES, HARDER SPAWNS (1.5× HP), FAST CHESTS (0.5× interval), LOW HP (0.6× hpMax), SWARM DAY (1.35× target alive). Daily mode skips shop bonuses + ignores Hyper/Endless toggles, so the leaderboard is fair. Start-screen purple DAILY button shows today's date + modifier + personal best (kills · time); toggles `optDaily`. `commitDailyRun` resets the record at midnight (local day rollover) and toasts "★ NEW DAILY BEST" on a fresh PR.
- [~] **Mod loader / weapon registration API** — Explicit "far future" item. Deferred — not part of this shipping milestone.
- [x] **Manual aim option** — Options-menu MANUAL AIM toggle (`meta.optManualAim`). When on, autoaim + Volley fire toward the cursor world-position (via cheap ortho-camera unproject onto y=0) instead of nearest enemy. `getAimWorldPos()` in input.js does the projection from mouse client coords. Falls back to facing direction before first mouse-move. No reticle yet — system cursor reads as the aim point.
- [x] **Boss telegraphs + named mini-bosses** — New `src/bossTelegraphs.js` module. 3 named mini-bosses (GROTHAR THE GLUTTON / VEXMAW THE SHRIEKER / OBLIDOR, IRON COLOSSUS) + final boss THE NIGHTMARE. Each runs a 9s cycle (final boss 6s) with a 1.2s wind-up: a pulsing red ring grows under the boss to its full danger radius. On resolve: radial shockwave damages + knocks back the hero if inside, spawns an outward-expanding ring. Mini-boss 18dmg/7u, final 26dmg/9u. Banner names + tells make boss fights have real beats; player can dash through the wind-up. Rings cleaned on death / restart.

- [x] **Gold-tinted chest sparks**: `spawnMagnetSpark(x, y, z, color)` now accepts a hex color. Spark InstancedMesh got `instanceColor` per-instance attribute. Chest pickup spawns gold (#ffe14a) sparks; gem magnetize keeps default cyan.

## Iteration log
(Each fire appends here: date — pass name — what changed)

- 2026-05-13 — Balance audit + README/biome close-out — `BALANCE.md` ships a math-derived audit (single config tweak: `difficultyMaxSec` 1800 → 1200 so dragons appear in the last 3 min of a normal run rather than only in Endless). Existing README confirmed presentable; 2nd biome closed via Twilight Hollow stage system. Mod loader explicitly deferred.

- 🏁 **SHIPPING COMPLETE** — Active queue cleared. Remaining items are either explicit "far future" (Mod loader) or play-test follow-ups flagged in `BALANCE.md`. Halting the autonomous loop.

- 2026-05-13 — Perf HUD (F3) + PERF.md — new `src/perfHUD.js`: F3 toggle (also `window.kkPerf()`), reads renderer.info + state arrays, 4Hz repaint, FPS color band (green/amber/red). `renderer.info.autoReset` switched off + manual `info.reset()` in renderFrame so the counters span both passes. New `PERF.md` documents the 16.6ms target, per-subsystem live-entity counts, the 8 architectural perf rules, profiling procedure, and watch-list of soft spots.
- 2026-05-13 — Twilight Hollow scatter pass — env.js now classifies scatter props with a `_stageTag` userData field. Live trees (140) + bushes (180) are tagged `forestOnly`; an extra 80 dead trees + 60 rocks are tagged `twilightOnly` and start hidden. `applyStageTint` flips visibility per stage so Forest stays lush + Twilight reads as a sparse, gnarled hollow with extra deadwood and stone outcrops. Combined with the brown_mud PBR ground, the two stages now feel like genuinely different places — same data layout, no rebuild cost.
- 2026-05-13 — Twilight Hollow real PBR ground — downloaded Poly Haven `brown_mud` 1k (CC0: diff + rough + nor_gl) to assets/sprites/brown_mud/. env.js now preloads both texture packs into `groundPacks = { forest, twilight }`; `applyStageTint` swaps `material.map / roughnessMap / normalMap` in place to whichever pack matches `stage.id`. Twilight tint softened to 0xb6bccc so the natural mud reads through with just a cool desaturation. Forest and Twilight now visually distinct beyond a recolor — actually different materials underfoot.
- 2026-05-13 — Stage select — `STAGES` config + `selectedStage` persistence. Twilight Hollow unlocks on first victory (1.30× enemy HP, final boss at 12:00, cooler ground+fog tint). Start-screen picker shows side-by-side chips with locked state. env.js gained `applyStageTint(stage)` to retint the ground material + scene fog on demand. spawnDirector and enemies.js read `state.run.stageFinalBossAt` / `stageHpMul`. Daily forces stage 1 for fair leaderboards.
- 2026-05-13 — Passives slot — new `src/weapons/passives.js`: 8 named passives w/ idempotent per-level apply. 6-slot cap; mixed into level-up rolls alongside weapons & evolutions; cards get a cyan PASSIVE LV N accent. Armor wires through new `statMul.dmgTaken` in `takeDamage`; Pummarola via `regenPerSec` in `updateHero`.
- 2026-05-13 — Map secrets (breakable logs) — new `src/destructibles.js`, 18 InstancedMesh logs scattered 12-60u from origin. Break on dash / bomb / boss shockwave; drop gem + 30% heart + 8% chest. Wired into hero.js dash, pickups.js bomb effect, and bossTelegraphs.js shockwave resolve.
- 2026-05-13 — Final-boss affix relics — `rollRelic()` generates a 2-affix item from a 7-stat AFFIX_POOL; quality average maps to Common/Rare/Epic/Mythic tier with bespoke accent color. Drops on final-boss kill, persisted to `meta.relics[]`, auto-equipped. Death-screen tier-bordered panel reveals the drop; start screen shows the equipped relic as an inline chip. `applyMetaUpgrades` applies relic affixes additively over character + shop bonuses.
- 2026-05-13 — Boss Rush mode — `state.modes.bossRush` overrides spawnDirector: mini-bosses at 25/75/135s, final at 200s; target-alive slashed to 4 so the run is bosses-not-swarm. Options toggle gated by `unlockedHyper`. Banner toast at run start.
- 2026-05-13 — Level-up / slot machine / banner / toast polish — Level-up modal: title rendered in title-case Cinzel ("Level Up") + new uppercase Inter subtitle ("Choose your path"). Reroll/Skip buttons routed through a new `qolBtn(label, accent, charges)` factory: display font with `× N` charge counter in JetBrains Mono, rounded ivory chrome. Slot machine: radial-gradient + blur backdrop, "Treasure" display title + Inter subtitle, reel cabinet now uses a single bordered panel with 3 inset reel windows (gradient + inset shadow), Take It / Double or Nothing buttons share the `slotBtnStyle()` factory. Banner: replaced the harsh 60-shadow neon text with a frosted glass strip (top + bottom ivory hairline + radial backdrop-blur), Cinzel Decorative 38px, fade-in animation. Achievement + Secret toasts: rebuilt with gradient panels + rounded corners + soft drop-shadow accents (amber for achievements, purple for secrets), display-font ALL-CAPS eyebrow ("Achievement" / "★ Secret Found"), display-font 18px name, lighter 11.5px body. Every "ALL CAPS HARSH NEON" element across the UI is now consistent with the new aesthetic.
- 2026-05-13 — Shop / Grimoire / Options refresh — all three modals now use the radial-gradient + backdrop-blur scaffold, Cinzel Decorative titles in title-case (Shop / Grimoire / Options), uppercase Inter subtitle under each ("Permanent upgrades…", "Evolution recipes…"), and consistent close-button chrome (`Close · Esc`). **Shop**: TREASURY row uses display font + JetBrains-Mono coin figure; upgrade cards rebuilt as 56-column-icon + body grid with level-pip strip (gold / dim), gradient panels, hover lift, mono cost line, full-amber MAX state. **Grimoire**: discovered evolutions in gold-bordered gradient cards with display-font headers + "Recipe ·" body; undiscovered ones desaturated to gray with explicit hint copy. **Options**: panel uses shared `.kk-panel` chrome; rows are Inter-uppercase labels w/ 0.24em tracking; toggles unified through `toggleStyle(accent)` factory so Music/Manual Aim/Hyper/Endless all share the rounded ivory-bordered button style; labels reformatted to title-case.
- 2026-05-13 — Start/death panel polish — Start screen now has ornamental gold SVG flourishes above and below the title (vector, no asset dependency, drop-shadowed). Title rendered in title-case Cinzel Decorative instead of ALL CAPS. Meta line restyled with token-spaced uppercase Inter + gold figures, "first run" badge if no prior runs. Character picker cards rebuilt: gradient panels, ivory hairline borders, 36px icons with drop-shadow, Cinzel name + small descriptive body, transform-on-hover lift. Start screen buttons unified through a `ghostBtn()` style factory (display font, 0.28em letter-spacing, soft inset highlight + 8/20 drop shadow). DAILY button restructured with display header + small uppercase modifier line + mono best-line. Death screen stats moved to a CSS grid (label/value columns) with mono numerics, divider rule between run and lifetime sections, ★ BEST tag on new records. Damage-by-source panel rebuilt as a 5-column grid with cyan→amber gradient bars and pill borders. Retry button uses display font + rounded panel chrome.
- 2026-05-13 — Menu typography refresh + real shadow pass — **Fonts**: Google Fonts loader injects Cinzel Decorative (display) + Inter (body) + JetBrains Mono (numerics) at boot. Theme `F.display/F.body/F.mono` font stacks replace every `'Courier New', monospace` in ui.js (both injected CSS + inline cssText). Palette softened (warm parchment text `#f5efe1`, gold `#ffd27f`, mint `#7fffe4`, rose `#ff7ad8`); kicked harsh neon glow in favor of layered drop-shadows, subtle inset highlights, and `${C.edge}` (warm-ivory hairline) for borders. Start/death/level-up/modal all use frosted radial-gradient backdrops with `backdrop-filter: blur(8-10px)`. HP bar redesigned as a pill with gradient fill. Cards have fade-in animation, soft 18px shadow lift on hover, real border-radius. New `.kk-panel` shared chrome for shop/grimoire/options. **Shadows**: renderer shadow map turned on (PCFSoftShadowMap, radius 4). Sun is the only caster (2048² map, bias −0.0004, normalBias 0.04, 80u frustum). Sun + target now follow the hero each frame from main.js so the shadow camera always frames the action. Hero + chests + elites/mini/final bosses castShadow=true; swarm enemies keep blob shadows for perf. Ground (forrest_ground_01 PBR) receives. Hero blob removed (real shadow takes over).


- 2026-05-13 — HP heart + star pickups — new `src/pickups.js` with two InstancedMesh pools (32 hearts, 16 stars), emoji-canvas textured. Hearts heal 25 HP on contact, stars magnetize all on-screen gems. Drop on kill: hearts 5%/100% elite, stars 2%/50% elite. Wired into main loop + `enemies.killEnemy`.
- 2026-05-13 — Weapon evolutions — `state.hero.fillerCounts` tracks picks. EVOLUTIONS map in weapons/index.js gates Toxic Halo (orbitals + 3 magnet) and Storm (chain + 3 cooldown). Eligible evolutions prepended in `weaponChoices`. Evolution cards have gold border. Orbitals tick reads `inst.evolved` for 2.5× dmg + 1.15× radius + poison DoT (1s, dmg/2 dps); chain reads it for 2× dmg, +3 chains, 0.3s cd. New ideas added: more weapon evos, evo banner, pickup magnetism, gem color tiers, compact damage numbers.
- 2026-05-13 — Animated enemy roster + HDRI + jump — Downloaded 16 Quaternius "Ultimate Monsters" GLBs (CC0, animated: Mushnub/Cactoro/Goleling/Orc-New/Demon-New/Blue-Demon/Wizard/Yeti/Pink-Slime/Ghost/Spider/Wolf/Dragon-New/Dragon-Evolved/Mushroom-King/Goleling-Evolved). Rewired assets.js preload to use them. Added `getClips/findClip/upgradeMaterials` to assets.js — auto-converts Lambert/Phong to MeshStandardMaterial with envMap for proper PBR. `_makePooledMesh` attaches an AnimationMixer playing walk/run/idle by name. `updateEnemies` ticks mixers. Auto-fit normalizes the varied source-asset heights to 2.0 world units × tier.scale. Added Poly Haven `approaching_storm_1k.hdr` as `scene.environment` for soft ambient reflections. JUMP — Space-bar edge-trigger in input.js (`consumeJump`), gravity/velY in state+hero, hero mesh rises with pos.y. New tiers ready for use: wizard/ghost/spider/wolf/dragon_evo.
- 2026-05-13 — Weapon + pickup visual upgrade + glow dial-down — Orbitals: core sphere + additive glow plane (gold/green when evolved). AutoAim projectile: cyan core + glow halo. Chain: LineBasicMaterial → TubeGeometry (real thickness). Hearts: 3D extruded heart Shape with red emissive. Stars: extruded 5-point star with gold emissive. Both spin + bob, use MeshStandardMaterial so HDRI ambient picks them up. User said too much glow → reduced glow plane sizes (1.4→0.7, 1.6→0.8), opacity 1.0→0.55, bloom strength 0.55→0.30, threshold 0.75→0.85, chain blending additive→normal.
- 2026-05-13 — New enemy tiers wired — Spider (minD 1.2, fast squishy), Wolf (2.0, medium fast), Wizard (3.0, slow ranged-prep), Ghost (4.0, translucent blue). Added POOL_PREWARM entries. Ghost mesh gets transparent blue tint applied in `_makePooledMesh`. New ideas added: enemy damage flash, hero airborne walk damp, pickup attract magnetism.
- 2026-05-13 — Hero airborne polish + enemy damage flash — Hero: while !grounded, bob/sway dampened to 0.15×, small forward tilt added for leap silhouette. Enemy flash: every pooled mesh now gets per-instance cloned materials (cloneCached shares by default) with original emissive cached in `mesh.userData.flashMats`. damageEnemy stamps `_flashUntil = time+0.08`. Update loop applies emissive white at intensity 1.6 while flashing, restores on edge. Spawn-from-pool restores in case of stale state.
- 2026-05-13 — Mini-boss waves @ 4/8/12 min — `STAGE.miniBossSchedule = [240, 480, 720]`. Spawn director shows 4s "ELITE INCOMING" warning banner, then spawns at due time with "MINI BOSS" banner. Picks an allowed elite (giant/dragon/etc.) at HP×3, scale ×1.4. Removed the old random `bossIntervalSec` event in favor of this scheduled, announced version.
- 2026-05-13 — Pivot fix + damage variance/crits/compact numbers — User reported enemies invisible: likely Quaternius models have origin at chest height so they sunk below ground after auto-fit. Now compute `bbox.min.y` after scaling and stash `yOffset` to apply on spawn so feet sit at ground. Added one-time `[enemy:<key>]` console log. `DAMAGE = {variance 0.20, critChance 0.08, critMul 2.0}` config; damageEnemy now rolls variance + crit, passes real crit flag to damageNumbers. Crits = yellow + bigger + bolder + +0.14s flash + +0.20 shake. Compact damage display (1.2K / 1.2M).
- 2026-05-13 — Forest bugs + art director shader pass (1-3) — Downloaded 10 Poly Pizza insect GLBs: Beetle, Ladybug, Grasshopper, Butterfly, Bee, Cockroach, Ant, Wasp, Caterpillar, Mantis (CC-BY Poly by Google + CC0 Quaternius Wasp). Wired into ENEMY_TIERS with forest-themed stats — high-weight bugs dominate the spawn pool, monsters take lower weight. Bumped POOL_PREWARM accordingly. Art director consult shipped first 3 items: tone mapping Linear → ACESFilmic exposure 1.05; light stack rebalanced (drop ambient, hemi 0.35 cool/warm split, warm 2.2 key from steeper angle, cool 0.25 fill); HDRI envMapIntensity 0.55→0.70. New `src/blobShadows.js` — InstancedMesh of 320 soft-radial-gradient dark circles, one per hero + active enemy, scaled by mesh size. ~0.3ms. Saved remaining 5 art director items to ART_DIRECTOR_PLAN.md for loop iterations.
- 2026-05-13 — Art director #4 (selective bloom layers) — `BLOOM_LAYER = 1` exported from postfx.js. New `bloomComposer` renders layer 1 ONLY into a texture (with black bg + fog disabled), main composer adds it back via additive composite shader. Threshold 0, strength 0.7, radius 0.5. Bloom layer enabled on: kill rings, magnet sparks, orbital core/glow, projectile core/glow, chain tubes, chest halo. Hero/enemies/ground no longer "accidentally bloom" — glow is deliberate-only.
- 2026-05-13 — Art director #5 (rim light) + pickup magnetism — Rim: `_injectRim` adds onBeforeCompile shader injection to every standard material. View-space rim term `pow(1.0 - max(dot(N,V), 0.0), 2.4) * 0.35 * #aaccff` added to outgoingLight. Patches `<opaque_fragment>` (or `<output_fragment>` fallback). Characters now have cyan backlight separation. Pickup magnetism: hearts/stars get vx/vz, drift toward hero with accel 28 + max 22 when within 2.5× pickup radius.
- 2026-05-13 — In-place death-screen RETRY — `window.kkRestart` exposed by main.js. Returns active enemies to pools, hides their meshes, clears projectiles+webs+chests+pickups, resets state, restores hero material opacity (death-anim mutated it), reinit spawn director, re-acquire orbitals. Death screen now shows a big RETRY [R] button. R/Space/Enter all trigger restart. Avoids the 2-3s page reload that recompiles+reloads all GLBs/HDRI.
- 2026-05-13 — Procedural bug animation — Static Poly-by-Google bug GLBs have no clip data; added per-tier `procAnim` ('crawl' / 'flap' / 'hover' / 'hop' / 'inch'). Applied only when `mesh.userData.hasClip` is false. Beetles & ants now scuttle (Z-wobble + tiny bob), butterflies wing-flap (X-scale + big bob), bees/wasps hover-jitter, grasshoppers hop, caterpillars accordion-inch. Spawn captures `_baseY` + `_baseScale` for stable transforms. Diagnostic console log per enemy key.
- 2026-05-13 — Hero damage + level-up flash overlays — Two DOM radial-vignette overlays under ui.js: red `kk-dmg-overlay` flashes 90ms on every hit; cyan `kk-lu-overlay` pulses 200ms on level-up. Wired into `hero.takeDamage` and both level-up paths (hero.js + xp.js).
- 2026-05-13 — Beetle clipping fix + tamer proc anims — Root cause: spawnEnemy was multiplying `yOffset` by `tierConfig.scale` but yOffset was already in world units (computed post-scale at pool time). Removed the multiplier. Also `_baseY` for procAnim was double-scaled — fixed. Then rewrote `crawl` to a scale-pulse (no body tilt) so beetles don't tip through the ground. Toned `flap`, `hover`, `hop`, `inch` amplitudes for ground-consistent feel.
- 2026-05-13 — Shader-based vertex animation for static bugs — Confirmed via GLB-JSON inspection: Quaternius models (Mushnub/Cactoro/Wasp/Spider) have animations; Poly-by-Google bugs (Beetle/Ladybug/Ant/Butterfly/etc.) have ZERO animations. New `injectVertAnim()` in assets.js — onBeforeCompile vertex-shader injection per kind: 'crawl' (bottom-half verts wave in alternating phase = leg shuffle), 'flap' (side verts oscillate Y opposite = wing flap), 'hover' (rapid micro Y-jitter), 'inch' (body squash-wave along X). Only applied to meshes with zero animation clips. Per-frame `vertTime` uniform updated in updateEnemies. Each beetle now has real leg motion, butterflies real wing flap, no bones needed.
- 2026-05-13 — XP gem color tiers + chest pickup burst — Gems now use per-instance color attribute. value 1 → cyan 1.0×, value 5 → magenta 1.5×, value 20+ → gold 2.4×. Elite/boss drops visually distinct. Chest pickup: 10-spark radial burst at random angles, bigger yellow ring, +0.5 bloom boost.
- 2026-05-13 — Art director #6 (height fog tint) + #7 (per-material roughness) — PostFX `fogTint` + `fogAmount` uniforms blend screen toward dark green-blue at the top (smoothstep Y gradient). `upgradeMaterials(root, env, roughness)` now accepts a roughness override: hero passes 0.92 (matte plush fabric), elite enemies 0.55 (glossy menace), bugs 0.65 (chitinous sheen), default 0.85 unchanged.
- 2026-05-13 — Art director #8 (LGG color grade) + animated chest open — LGG: lift/gamma/gain Vec3 uniforms in PostFX (default cool shadows / warm gain). Main loop lerps `lift` toward red dread tint when final boss is alive. Chest open: on pickup, spawn `chest_open` GLB at chest pos, emissive gold, lift+fade over 1.4s before despawn.
- 2026-05-13 — HUD weapon icons + bug variant tint — Bottom-right HUD shows each owned weapon as `[emoji] LVn` (gold border + "EVO" label when evolved). Cells dynamic, repaint only on level/evo change. Bug variant tint: per pool-mesh ±9% HSL hue+lightness jitter on flashMats — every individual ant/beetle/cockroach now has its own shade so swarms look like crowds, not copies.
- 2026-05-13 — Wizard ranged attack — `wizard` tier gets a `ranged` config block. New `src/enemyProjectiles.js` module spawns/ticks projectiles (magenta core + bloom-layer glow halo). In `updateEnemies`, ranged enemies stop or backpedal inside range and fire on cooldown toward hero pos at spawn time. Projectile expires by ttl or hits hero → `heroTakeDamage`. Cleared on restart.
- 2026-05-13 — Tutorial overlay + evolution banner — First-run tutorial: 5s after start, shows a controls panel (WASD/Space/Shift/wheel/ESC), auto-dismiss 10s, click/key dismisses, persisted via `meta.seenTutorial`. Evolution banner: when an evolution first becomes eligible, `_announceEligibleEvolutions()` shows gold "★ EVOLUTION READY: TOXIC HALO" banner + bloom boost. Tracked per-run via Set, reset on restart.
- 2026-05-13 — Achievements + mini-boss rewards — 8 achievements (First Blood/Giant Slayer/Centurion/Treasure Hunter/Jackpot!/Champion/Evolved/Triple Threat) in `meta.ACHIEVEMENTS`. `unlockAchievement(id)` persists + returns def; `tryAchievement(id)` in ui.js toasts a right-side amber notification (slide in, 3.5s, slide out). Triggers wired at kill/elite/chest/jackpot/evolution/victory/3xMiniBoss. Mini-bosses now tagged `isMiniBoss` with guaranteed reward drop: chest + 2 hearts + 1 star.
- 2026-05-13 — UX cluster (music off / pickup ring / mini-boss timer / landing squash) — `optMusic` default `false` (user prefers silence). Cyan additive ring under hero sized to pickup radius × magnet stat via `updatePickupRing` each frame. HUD now shows "ELITE IN MM:SS" line. Hero landing squash: clamp(-velY/12) × duration 0.18s, scale.y down 25%, scale.x/z up 15%.
- 2026-05-13 — Volley + Tangle evolutions + gold sparks — AutoAim+damage×3 → Volley: +2 projectiles, 1.5× speed, +2 pierce, 1.4× dmg. Web+speed×3 → Tangle: 1.5× radius+duration, 0.7× slowMul, 0.7× cooldown. Spark InstancedMesh got per-instance color attribute; `spawnMagnetSpark(x,y,z,color)` accepts hex. Chest pickup bursts in gold #ffe14a.
- 2026-05-13 — README + camera shake calibration + community research — README.md shipped with controls/features/credits/architecture. Normal kill shake removed (was vibrating constantly), elite 0.55→0.35, player damage scales with severity (clamp(amt/30)). Damage flash overlay opacity 0.45..1.0 + duration 70..150ms by severity. **3 parallel research agents** harvested Steam reviews, r/VS, Brotato/HoT/DMD/HoloCure threads. Consensus must-adds queued at top of roadmap: meta-progression shop, character roster, level-up reroll/skip/banish, evolution Grimoire discovery, secret unlocks, 2nd biome, run-summary, Hyper mode, DPS overlay, VFX slider, boss rush, endless mode, map secrets, passives slot, pickup variety, affix loot, daily seed, manual aim, boss telegraphs.
- 2026-05-13 — Meta-progression shop — #1 community-validated gap. `SHOP_UPGRADES` in meta.js: Iron Resolve (HP+), Lodestone (magnet+), Swift Boots (speed+), Sharpened (dmg+), Quick Study (XP+), Lucky Charm (chest spawn rate+). 5 levels each, quadratic 25→400 coin cost. Start-screen "SHOP" button opens modal; ESC closes. `applyMetaUpgrades()` runs at game-start and at retry. Growth applied to XP gain in xp.js, Luck applied to chest interval in spawnDirector. Coins are no longer dead currency — they fuel a real run-over-run power growth loop.
- 2026-05-13 — Level-up reroll/skip + DPS overlay — Two universal QoL asks. `state.hero.rerolls` and `state.hero.skips` start at 1 per run. Level-up modal shows REROLL [Q] (regenerates choices) and SKIP [E] (closes without applying) buttons when player has charges. Q/E keyboard shortcuts active while modal open. DPS overlay: sliding window of damage events in `state.run._dpsWin`; HUD shows rolling 5s-mean DPS in green (1.2K format for large).
- 2026-05-13 — Hyper + Endless modes + VFX slider — On first victory: `unlockedHyper` + `unlockedEndless` flip to true; banners toast. Options-menu toggles (only shown post-unlock). Hyper: 1.5× enemy HP/spd/dmg + 1.5× coin gain. Endless: final boss kill drops 2 extra chests but the run continues. VFX slider 0..1 multiplies bloom strength per frame to reduce late-game visual soup.
- 2026-05-13 — Pickup variety: bomb / freeze / chicken — 3 new pooled pickups. Bomb (red sphere) AoE 50 damage in 18u with kill ring + 16 outward sparks. Freeze (cyan octahedron) spawns a 60-unit web at 0.15 slowMul for 3s — global slow via existing web system. Chicken (warm sphere) restores full HP. Drop rates: 0.3% / 0.5% / 0.2% on normal kills; elites add +8% freeze / +5% chicken; mini-bosses guarantee bomb in their reward kit.
- 2026-05-13 — Evolution Grimoire codex — `meta.discoveries` persists evolution IDs the player has stumbled into. Start screen "GRIMOIRE" magenta button opens a modal grid of 4 evolutions. Found ones reveal name/icon/desc/recipe; undiscovered show "???" with a hint. Discovery fires silently when eligibility detected (via `_announceEligibleEvolutions`). ESC closes.
- 2026-05-13 — Manual aim option — Options toggle `optManualAim`. autoaim/volley now reads `getAimWorldPos()` from input.js when on — projects cursor NDC through the orthographic camera onto y=0 (manual unproject, no Raycaster allocs). Falls back to facing direction pre-mouse-move.
- 2026-05-13 — Daily seeded challenge — `dailyChallengeConfig()` picks today's character + 1-of-5 modifier deterministically from a date hash (xfnv1a). DAILY toggle on start screen shows today's setup + personal best. `applyMetaUpgrades` skips shop bonuses when daily is on and forces the daily character; modifier wires into enemy HP (`dailyHpMul`), target alive count (`dailySpawnMul`), chest interval (`dailyChestMul`), or hero hpMax (LOW HP). `commitDailyRun` resets at local midnight + toasts on new best.
- 2026-05-13 — Air dash combo — Dashing while airborne triggers a 0.4s phase dash. `_airDashUntil` window suspends gravity and zeros velY at dash start; the gravity block in updateHero is gated behind `airDashing`. Pre-evolution phase-dash flavor.
- 2026-05-13 — Run-summary breakdown (per-weapon DPS) — `damageEnemy(enemy, dmg, source)` now optionally accepts a source id. orbitals/toxic_halo, chain/storm, autoaim/volley, dash, bomb, and the poison DoT all tag their damage. `state.run.dmgByWeapon` accumulates per source. Death screen renders a sorted breakdown with cyan progress bars: total / percent / DPS per source. Resets cleanly on retry via resetState.
- 2026-05-13 — Boss telegraphs + named mini-bosses — new `src/bossTelegraphs.js`. 3 named mini-bosses + named final boss. Each runs a 9s/6s cycle with a 1.2s wind-up: pulsing red ring grows to full radius, then resolves into a damaging shockwave (mini 18dmg/7u, final 26dmg/9u). Knocks hero clear of the radius on hit (telegraphed, dashable). Per-enemy state on the enemy object; rings disposed on kill/restart.
- 2026-05-13 — Secret unlocks + per-character placeholder visuals — 7 secrets w/ purple "★ SECRET FOUND" toast (Bug Lord, Untouchable, Faster Than Light, Flawless, House Always Wins, The Long Night, Hoarder). Lifetime counters (`meta.lifetime.bugKills/jackpots/coinsEverEarned`) + run-scope tracking (`flawless`, `noDmgKills`, `speedrunChecked`). Checks fire from killEnemy / takeDamage / level-up / slot jackpot / per-frame time. Each character (Kitty/Boom/Webspinner/Sniper) now ships a placeholder differentiation: per-char `tint` hex + `scaleMul` multiplied into the shared GLB on spawn (material cloned to keep the cache clean). Boom = orange-red 0.92×, Webspinner = violet 1.08×, Sniper = pale green 0.96×. `rebuildHero()` called on start-press + restart so picker selection visibly applies.
- 2026-05-13 — Character roster (4) — Kitty / Boom / Webspinner / Sniper. Each overrides starter weapon + hpMax + stat multipliers. Start-screen picker shows cards (locked ones show 🔒 + unlock condition tied to existing achievement IDs). `selectedChar` persisted in meta. `applyMetaUpgrades` applies char stats first; shop upgrades stack on top.
